<h1>All lists</h1>

<% @lists.each do |list| %>
  <div>
    <p><%= link_to list.name, list_path(list) %></p>
  </div>
<% end %>

<%# <%= link_to new_list_path do %>
<%# <% end %>
<%= link_to "New list", new_list_path %>
